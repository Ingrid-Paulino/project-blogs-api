// const { User } = require('../../models');

// const create = async (reqBody) => {
//   const model = await User.create(reqBody);

//   if (model) return { status: 401, message: 'Product already exists' };

//   return model;
// };

// module.exports = { create };
